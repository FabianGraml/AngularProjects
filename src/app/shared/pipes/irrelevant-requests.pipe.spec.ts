import { IrrelevantRequestsPipe } from './irrelevant-requests.pipe';

describe('IrrelevantRequestsPipe', () => {
  it('create an instance', () => {
    const pipe = new IrrelevantRequestsPipe();
    expect(pipe).toBeTruthy();
  });
});
